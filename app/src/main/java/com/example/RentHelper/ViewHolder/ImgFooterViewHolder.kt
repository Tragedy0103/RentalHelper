package com.example.RentHelper.ViewHolder

import android.view.View
import com.example.RentHelper.Tool.BaseViewHolder
import com.example.RentHelper.Tool.IType

class ImgFooterViewHolder(itemView: View) : BaseViewHolder<IType>(itemView) {
    override fun bind(item: IType) {
        itemView.setOnClickListener{

        }
    }
}